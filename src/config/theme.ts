import colors from '~config/colors'

const { primary, button } = colors

export default {
  textColor: {
    primary,
  },
  buttonText: {
    color: button.text,
  },
  containedButton: {
    backgroundColor: primary,
  },
}